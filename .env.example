# Solana RPC endpoint (required)
SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Wallet: set ONE of the following (USER_KEYPAIR takes precedence).
# If neither is set, the bot falls back to ~/.config/solana/id.json.
# Path to a JSON secret-key file (solana-keygen format)
USER_KEYPAIR=/home/you/.config/solana/id.json
# BIP39 mnemonic phrase
#SOLANA_MNEMONIC=

# Execute real trades. false = simulation mode (quotes only, no swaps)
ENABLE_TRADING=false

# Optional: Jupiter API base URL (free tier; paid tier: https://api.jup.ag/swap/v1)
#JUPITER_API_BASE_URL=https://lite-api.jup.ag/swap/v1

# Optional strategy overrides (defaults shown)
#MM_WAIT_TIME_MS=60000
#MM_SLIPPAGE_BPS=50
#MM_PRICE_TOLERANCE=0.02
#MM_REBALANCE_PERCENTAGE=0.5
# Minimum trade value in USD (was MM_MINIMUM_TRADE_AMOUNT, token units)
#MM_MINIMUM_TRADE_VALUE_USD=1

# Priority fee in lamports attached to swap transactions
#MM_PRIORITY_FEE_LAMPORTS=200000
# Skip RPC preflight simulation when sending swaps (true = faster, riskier)
#MM_SKIP_PREFLIGHT=false
